name: ruby_text
description: "A RubyText widget to achieve the furigana effect."
version: 0.0.2
homepage: https://github.com/najeira/flutter_ruby_text

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: any

dev_dependencies:
  build_runner: ^2.4.9
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.4.7

flutter:
